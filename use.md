# 模型路由选择模型
在配置模型路由时，选择上游模型名时给下拉框，从该账号的若干上游模型中选择一个填入

# 生成api-key生成接口改造
现在，请求生成api-key时，对方必须指定分组（输入分组名）
plus：现在，如果某个分组为专属分组，那就必须在用户管理-更多-分组，勾选那些专属分组，界面上用户才能在key上选到这些分组，这个限制希望在apikey接口上也要有

# 查询某个分组下的路由列表
开放接口
输入鉴权的token

入参：分组名
返回：分组-路由别名-上游模型名的大json
