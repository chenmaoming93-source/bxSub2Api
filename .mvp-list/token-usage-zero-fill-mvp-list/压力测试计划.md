# 系统目的
1. 统计全行token用量
2. 控制模型用量和路由逻辑
3. 方便全行接入

# 压测计划
目前有两个降级逻辑：
达到限额降级
报错降级

我们要基于当前存在的两个策略进行压测

# 场景
1. 组内有多个模型账户（4~5个）


2. 报文大小维度：传输文字/图片（本质就是更大的报文），理论文字上限1M，按照这个测能cover任何大小


3. 输入并发维度：


4. 模型配置并发维度：？？？

5. 流式调用/非流式调用
流式是长链，工具不支持流式？
另外，有大量线程持续执行流式调用，资源消耗极大

# 现有问题/待办
1. 压测端怎么统计流式调用的信息（jmeter在接收第一个返回时就结束了）
2. mock 4~5个假模型，伪装为具有多模态的模型
3. 报文大小：1M（最小）~10M（图片）
4. 并发数：1000为起点
5. 模型路由组合策略：分组都是1个分组
a. 没触发限额，直接调用第一个
b. 触发限额，降级到最后一个

6. 配置账户并发数保证不被限制